import {createContext} from 'react';

// export const ApplicationContext = createContext(false);


// Initializing the context with a detailed default structure
export const ApplicationContext = createContext(null);